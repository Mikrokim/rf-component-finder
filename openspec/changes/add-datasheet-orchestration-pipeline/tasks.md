## 1. Data model: datasheet link on Candidate

- [x] 1.1 Add `datasheet_url: str | None = None` to `Candidate` in `rf_finder/models.py` — must be the LAST field, since the others have no defaults (defaulted so existing constructions are unchanged)
- [x] 1.2 Update the `core-data-models` field list / docstring to mention `datasheet_url`
- [x] 1.3 Test: a `Candidate` built without `datasheet_url` has `datasheet_url is None`; one built with it exposes the link

## 2. Datasheet fetch-by-URL (URL-only)

- [x] 2.1 Rewrite `rf_finder/datasheet/pdf.py` to URL-only: add `DatasheetFetchError`, an internal pure parse core `_text_from_stream(source)` (reusing `_join_page_text`/`pdfplumber`), and the public `datasheet_text_from_url(url)` that fetches with `httpx` into `io.BytesIO` (no temp file); remove the local-file `datasheet_text_from_pdf`
- [x] 2.2 Raise `DatasheetFetchError` on network/HTTP error, non-PDF response (`%PDF`/Content-Type guard), or unparseable PDF; rely on project `truststore` TLS (never `verify=False`)
- [x] 2.3 Update exports in `rf_finder/datasheet/__init__.py` (`datasheet_text_from_url`, `DatasheetFetchError`; drop `datasheet_text_from_pdf`)
- [x] 2.4 Update `tests/test_datasheet_pdf.py`: keep the `_join_page_text` tests; stub `httpx.get` (and the parse core) to test a successful URL fetch, an HTTP error, and a non-PDF response — no real PDF or network

## 3. Adapter contract: on-demand datasheet-link resolution

- [x] 3.1 Add `resolve_datasheet_url(self, cand: Candidate) -> str | None` to the `Adapter` ABC in `rf_finder/adapters/base.py` as a NON-abstract default returning `cand.datasheet_url` (so all 12 existing adapters satisfy it unchanged)
- [x] 3.2 Document the contract on the default: return `None` rather than raise when the link cannot be resolved; never fetch per-part pages from `search()`; never fetch a robots-disallowed URL
- [x] 3.3 Test: the default returns the candidate's existing `datasheet_url` (and `None` when it has none) without making any request

## 4. Management layer: pipeline module

- [x] 4.1 Create `rf_finder/pipeline.py` with `run_pipeline(spec, *, on_source=None)`, reusing `search._sources_for` and the `on_source` hook
- [x] 4.2 Stage 1 (retrieve): gather candidates from every supporting adapter, keeping each candidate's producing adapter (`(adapter, candidate)` pairs) so later stages know whom to ask; one bad source skipped via `on_source("error"/"empty"/"ok", ...)`
- [x] 4.3 Gate 1: `verify()` each candidate; keep only those with no `FAIL` verdict (every site-provided parameter passes; `UNKNOWN` deferred)
- [x] 4.4 Stage 3 (resolve): for each Gate 1 survivor that has at least one `UNKNOWN` requested param, call `adapter.resolve_datasheet_url(cand)` and carry the result via `dataclasses.replace(cand, datasheet_url=...)`. Survivors with nothing missing are skipped — the link is only enrichment's input, so resolving theirs would cost a request for a datasheet never read
- [x] 4.5 Contain resolution failures per candidate (adapter raising despite the contract): treat as `None` and continue the run
- [x] 4.6 Enrichment: for each survivor compute the `UNKNOWN` requested params; if `datasheet_url` is set after resolution, fetch text, call `extract_rf_parameters(text, missing)`, map via `to_raw_params`, and merge into a `dataclasses.replace` copy with `source="datasheet"` (never overwriting table values); skip survivors with nothing missing entirely
- [x] 4.7 Contain per-candidate enrichment failures (`DatasheetFetchError`, extraction/LLM errors) so the run continues; keep `requests`/LLM imports lazy
- [x] 4.8 Gate 2: re-`verify()` each enriched candidate against the same `spec`; assign `match` (no `FAIL`, no `UNKNOWN`), `not-verified` (no `FAIL`; every remaining `UNKNOWN` is from a datasheet-access failure; coverage ≥ 0.80), or dropped (any `FAIL`; an `UNKNOWN` from a datasheet that was read but silent; or a `not-verified`-shaped candidate below 0.80) — a `FAIL` always drops
- [x] 4.9 Track datasheet accessibility per candidate (the five "no access" conditions; condition 1 is "no `datasheet_url` AFTER resolution") so Gate 2 can tell an access-failure `UNKNOWN` (→ `not-verified`) from a silent-datasheet `UNKNOWN` (→ dropped)
- [x] 4.10 Compute `coverage = (requested params with PASS) / (total params in spec)` and admit a `not-verified` candidate only when `coverage >= 0.80` (never rescues a `FAIL`)
- [x] 4.11 Tag each accepted candidate with its outcome and expose the result fields — product name (`model`), manufacturer, product URL (`url`, unchanged from today), outcome — keeping BOTH `source` and `datasheet_url` out of the result contract
- [x] 4.12 Contain a per-candidate grading failure: `run_pipeline` calls `_grade` with no guard (`pipeline.py:59`), so an exception raised inside `verify()` for ONE candidate aborts the WHOLE run — contradicting D6 ("one adapter raising, one PDF failing, one extraction erroring never aborts the run"). `_retrieve`, `_resolve` and `_enrich` are each already contained; the grading step is the gap. Wrap the `_grade` call so a raising candidate is skipped and the rest complete. Reproduced live: a `ParamConstraint` whose unit the ontology cannot convert (e.g. `"C"` instead of `"degC"` for Temperature) makes `to_canonical` raise `ValueError` out of `verify()`. Test: one candidate whose verification raises does not prevent the others from being returned

## 5. Wire front-ends to the management layer

- [x] 5.1 Point `rf_finder/__main__.py` at `run_pipeline` (result set is now `match` + `not-verified`, each tagged with its outcome)
- [x] 5.2 Point `rf_finder/ui/gui.py` at `run_pipeline` and surface the `match`/`not-verified` outcome. The results view is otherwise UNCHANGED: no new column, and its link column keeps showing `c.url` and keeps the double-click deep-link exactly as today
- [x] 5.3 Re-head the existing link column: `gui.py:350` heads the `url` column **"Datasheet URL"**, but it is filled with `c.url` — the product page (`gui.py:331`), which is also what double-click opens (`gui.py:334`). The column always was the product page and stays the product page; only the heading is wrong. Re-head it to **"Product URL"**. No behavior change — same value, same double-click
- [x] 5.4 Keep `search_and_verify` available for the table-only tests

## 6. Per-source verification pass (prerequisite for §7)

Task 7 cannot be written per adapter until each source is classified. Mini-Circuits proved the "the link is in the table" assumption fails silently. **Verify each source against a live page — not research, not fixtures.**

For each source record: (a) case 1 (link on the already-scraped page) / case 2 (product page only) / case 3 (no datasheet); (b) for case 2, the robots-ALLOWED product-page URL template; (c) whether `robots.txt` allows fetching the PDF path — the pipeline now downloads PDFs, which adapters never did.

### Results — case per source (verified live, 2026-07-20)

"Parts with a link" = how many of the parts the adapter emits carry a datasheet link **from this source**. It is NOT "how many parts have a datasheet" — see the note below the table. `—` = not counted (verified on sample parts only).

| Source | Case | Where the link is | Parts with a link | robots for the PDF | §7 work |
|---|---|---|---|---|---|
| Mini-Circuits | **2** | `dashboard.html?model=<urlencoded>` → `<a>` text `DATASHEET` | — | `/pdfs/` allowed | override `resolve` (7.1–7.2, 7.9) |
| Marki | **2** | `Datasheet` column → HTML page → `<a>` text `Download PDF` → `/assets/{uuid}/….pdf` | 123/123 | allowed, both hops | override `resolve` (7.5) |
| GuerrillaRF | **2** | detail page → direct `/includes/prodFiles/{sku}/{MODEL}DS.pdf` | — | allowed | override `resolve` (7.6) |
| RWM | 1 | JSON `Datasheet` field, absolute | 377/377 | `Disallow:` empty | move out of `url` (7.3) |
| VectraWave | 1 | `datasheet` row `<a href>`, **relative** | 32/47 | allowed | read inline + `or None` (7.4) |
| AmcomUSA | 1 | last row cell `<a>` → CloudFront PDF, absolute | — | `Allow: /` | read inline (7.6) |
| MACOM | 1 | `data-part` JSON `datasheetHref`, absolute | 991/1013 | allowed | read inline (7.6) |
| Microchip | 1 | MCP `datasheetUrl`, absolute | 77/90 | link API allowed; **the file on `ww1` is `Disallow: /`** | **DONE** |
| UMS | 1 | `a.doc-link`, absolute | — | allowed | read inline (7.6) |
| Qorvo | 1 | row `/products/d/{docid}` — serves the PDF directly (`octet-stream` + `%PDF`) | — | allowed | read inline (7.6) |
| Analog Devices | 1* | *derived* `https://www.analog.com/en/{model}/datasheet`; no literal link in the JSON | 482/482 (derived) | allowed (robots read via `/cdp/robots.txt`) | read inline (7.6.6) |
| 3rWave | **3** | nothing — no link, no PDF, no per-part page | 0/51 | n/a | **none** |

**A missing link does not always mean the part has no datasheet.** Checked live:
- **VectraWave (15 parts)** — genuinely none: their WooCommerce product pages carry no `.pdf` either.
- **MACOM (22 parts)** — genuinely none: 5 sampled product pages have **0** occurrences of "datasheet" and only a boilerplate footer PDF.
- **Microchip (13 parts)** — **NOT genuine.** 5 are ordering variants (`MMA043PP4/TR`, `MMA044PP3/TR`, `MMA047PP4TR`, `MMA085PP4`, `MMA085PP4E`) whose base part is in the SAME result set WITH a `datasheetUrl` — same die, same datasheet, zero extra requests to recover (see 7.10). And the `search_microchip_product_documents` MCP tool (arg is `query`, not `partNumber`) returns a real datasheet for at least `MMA047CP4` and `SST12LP17E-XX8E` — it serves the document **content** from `api.microchip.com`, which would also sidestep the `ww1` robots problem entirely (raised as an open question, not scoped here).

Cross-cutting findings:
- **All three case-2 sources hide the PDF behind an HTML wrapper** (Marki `/datasheet/`, GuerrillaRF `/products/DataSheet?…`, Mini-Circuits' silent link-less 200). §7.8's "a product page with no datasheet link returns `None` without raising" should run against all three in one shared shape.
- **A wrong-but-valid PDF is worse than no PDF**: it sets `datasheet_accessible=True`, so a silent param DROPS the candidate instead of yielding `not-verified`. Hence the exact-text selectors above (`Download PDF`, `DATASHEET`) rather than "the first `.pdf` href" — Marki's `Online Catalog` links are the live example.
- **Two sources misfile the PDF into `Candidate.url`** (RWM, VectraWave). RWM has no per-part page at all (use `product.html?category={id}`); VectraWave does (`/product/{model-lower}`, from `product-sitemap.xml`).

- [x] 6.1 Mini-Circuits — **done, verified**: case 2. Table `<a href>` is `modelSearch.html?model=X` which robots DISALLOWS; the allowed product page is `dashboard.html?model=<urlencoded>` (sitemap-listed), carrying an `<a>` with text `DATASHEET`. `+` MUST be encoded `%2B` or the page returns 200 with NO datasheet link (silent failure). `/pdfs/` is robots-allowed.
- [x] 6.2 RWM — **verified live: case 1**. The `api/all-products` JSON (already fetched by `search()`) carries a `Datasheet` field on **377/377** amplifier products, every value ABSOLUTE and on the same host: `https://www.rwmmic.com/index.php?r=pdf%2Fdownload&filename={MODEL}.pdf` — no extra request, no absolutize. Live fetch: `200 application/pdf`, body `%PDF-1.7`, 393 KB; the project's `datasheet_text_from_url` fetched+parsed RWLA1001 (1666 chars) **with normal TLS verification** — the adapter's `verify=False` is needed only for the API host handshake in this environment, NOT by `pdf.py`, so D5's "never `verify=False`" stands. robots.txt is `User-agent: * / Disallow:` (empty → everything allowed, PDF path included). Currently misfiled: `rwmmic.py:281` puts this PDF link into `Candidate.url`. **Finding for 7.3 — there is NO per-part product page:** `product.html` is a client-side SPA whose only deep links are per-CATEGORY (`history.pushState` → `product.html?category={id}`); `sitemap.xml` returns HTML (no sitemap). So `url` should become `https://www.rwmmic.com/product.html?category={category.id}` (the group's category id is in the same JSON) or plain `product.html` — not a per-part URL, which does not exist.
- [x] 6.3 VectraWave — **verified live: case 1**. The `datasheet` row of each transposed section table (same HTML `search()` already parses) holds a per-product `<a href>` to a PDF — **RELATIVE** (`/wp-content/uploads/YYYY/MM/{MODEL}-DS-RevX.pdf`), so `_abs_url` (`vectrawave.py:282`) is required; 0/32 are absolute. Coverage **32/47** amplifiers. Live: `https://vectrawave.com/wp-content/uploads/2025/02/VM042D-DS-Rev3.0-Ed1.1.pdf` → `200 application/pdf`, `%PDF-1.7`, 613 KB; the project's `datasheet_text_from_url` fetched+parsed it (10201 chars). robots ALLOWS it (`can_fetch('*', …)==True`; only `/wp-admin/`, `/wp-content/uploads/{wc-logs,woocommerce_transient_files,woocommerce_uploads}/` and `?add-to-cart=` are disallowed — a Yoast `Disallow:` block follows). **Gotcha for 7.4 — the empty-href trap:** the other **15** products (VM101D, VM102D/Q/F, VM164F, VM171DNEW, VM174D, VM002DNEW, VM205D, VM103D/Q, VM104D/Q, VM001DNEW, VM105Q) render `<a href="">Download</a>` — the `<a>` EXISTS but the href is empty, and selectolax returns `None` for it (`attributes.get('href','')` → `None`, not `''`). `_abs_url` maps that to `""`, so §7.4 must emit `datasheet_url = self._abs_url(...) or None` — an empty string would look like a link and must never become `_BASE + "/"`. Confirmed those parts genuinely have no datasheet: their WooCommerce product pages carry no `.pdf` either, so `None` is correct (→ D7 condition 1 → `not-verified`), no case-2 hop is worth adding. **Finding for 7.4 (`url`):** per-part product pages DO exist — `https://vectrawave.com/product/{model.lower()}` (from `product-sitemap.xml`, 70 entries; VM042D/VM101D verified 200) — a better `url` than today's whole-page `_PAGE_URL` fallback once the PDF moves out of `url`. Not every listed model is in the sitemap, so keep `_PAGE_URL` as the fallback.
- [x] 6.4 Marki — **verified live: case 2, NOT case 1** (the §7.5 assumption is wrong — see below). The search table's `Datasheet` column (header index 3, i.e. `data_headers[2]`) does carry an `<a href>` on **123/123** amplifier rows, but it is a RELATIVE link to an **HTML page**, not a PDF: `/products/{package}/amplifiers/{model-lower}/datasheet/` → `200 text/html`, body `<!doctype…`. Feeding it to `datasheet_text_from_url` would hit the `%PDF` guard and fail silently (the GuerrillaRF/Mini-Circuits trap). The real PDF lives one hop further, on that page, as `https://markimicrowave.com/assets/{uuid}/{MODEL}-{title}.pdf` — the `{uuid}` makes the URL **NOT constructable**, so the hop is mandatory. **Robust selector (verified on 4 parts, exactly 1 hit each):** the `<a>` whose text is `Download PDF`. Do NOT take "the first `.pdf` href" — every page also carries two site-wide `Online Catalog` links (`MM_Catalog_SMT_Die_*.pdf`, `MM_Catalog_Connectorized_Waveguide_*.pdf`) which are catalogues, not datasheets. Live end-to-end: ADM-11425PSM (`200 application/pdf`, `%PDF-1.4`, 1.4 MB, 9916 chars via the project's `datasheet_text_from_url`), AMM-11561CH (13516), AMM-11059CH (12819), AMM-10861PSM (10537). **Compliance — conflict resolved:** robots ALLOWS both hops (`can_fetch('*', …/datasheet/)==True` for all four parts, and `can_fetch('*', /assets/…pdf)==True`; the file is a long deny-list of named bad bots, `*` is unrestricted). `marki.py:44`'s "datasheet PDFs are not fetched programmatically" was a statement of the table-only adapter's SCOPE, not a robots prohibition — it must be updated when §7.5 lands, since the pipeline now does fetch them. Cloudflare: the same browser UA that works for `/search/` works for both hops. **Which URL the hop starts from — read it, do not construct it:** the first-hop page IS the href of the row's `Datasheet` column. Live, that href equals the row's product href + `/datasheet/` on **123/123** rows (zero exceptions), so it *could* be derived from `cand.url` — but deriving it would be a constructed URL, the exact Mini-Circuits failure mode. §7.5 therefore reads the `Datasheet` column's href from the row and carries it, so the adapter follows the site rather than a pattern.
- [x] 6.5 3rWave — **verified live: case 3, OQ-3W-6 still holds**. `/amplifier/` (200, 153 KB, not filter-blocked) renders 2 `table.tablepress` with **51 rows, of which 0 carry ANY `<a href>`** — no per-part link at all, so `threerwave.py`'s text-fragment fallback is the only URL. The page contains **0 `.pdf` hrefs** and **0 occurrences of the word "datasheet"** (case-insensitive, whole document). The site has no per-part pages either: `sitemap_index.xml` lists only post (10), page (18), kboard (1) and category (1) sitemaps — the 18 pages are top-level sections (`/amplifier/`, `/filter/`, `/passive/`, `/product/`, `/contact/`, …) with no product URLs. robots is `User-agent: * / Disallow:` (moot — nothing to fetch). **Conclusion: `datasheet_url` stays `None`, no §7 work follows.** Every 3rWave candidate with a site-missing requested param therefore hits D7 condition 1 → `not-verified` when it clears 80% coverage, else dropped. NB the adapter leaves `P1dB`, `IP3`, `MSL`, `Temperature` (and Size/length/width) with no column, so 3rWave parts will land in that path often.
- [x] 6.6 AmcomUSA — **verified: case 1**. The `allPnTable` row's LAST cell (empty header) holds an `<a>` pointing straight to an absolute CloudFront PDF (e.g. `http://d2f6h2rm95zg9t.cloudfront.net/.../AM001019SF_1H_….pdf`) — same HTML response as the row's other params, no extra request. Currently discarded (`amcomusa.py:325`, empty-header column skipped). robots.txt `Allow: /` (only `/admin`,`/privacy` disallowed); PDF is on a separate CloudFront CDN. Impl: read the last cell's `.pdf` href into `datasheet_url` (identify by "last cell with an `<a>` to a `.pdf`", not a hardcoded index — columns differ per category).
- [x] 6.7 Analog Devices — **verified: case 1 (constructed URL), with an open fetchability risk**. The parametric JSON `search()` fetches (`/cdp/pst2/data/standard/3003.js`, 564 rows × 49 fields) carries NO literal datasheet link (scanned every row for `.pdf`/`datasheet`/`/media/` — none). BUT the datasheet URL is derivable from the part number, which IS in the JSON with the other params: `https://www.analog.com/en/{model}/datasheet` (user-confirmed for ADL6346B). So `search()` can set `datasheet_url` inline at ZERO extra requests — case-1-equivalent (link available without a per-part page fetch). Impl (§7): `datasheet_url = f"https://www.analog.com/en/{model}/datasheet"`. **OPEN RISK (not classification):** every httpx request to `www.analog.com` HTML/media/datasheet paths from this environment failed (`RemoteProtocolError`/`ReadTimeout`) while the `.js` data API worked — likely Akamai bot-protection (cf. www.microchip.com) or the Etrog proxy. If the block is real, the pipeline's PDF fetch will fail → ADI parts needing enrichment → not-verified. Unlike Microchip, ADI has NO alternative content API for datasheets. MUST verify the fetch from an environment that reaches analog.com before relying on enrichment. robots for the datasheet path: UNVERIFIED (robots.txt also unreachable from here).
- [x] 6.8 MACOM — **verified: case 1**. Each `data-part` JSON blob (already fetched/parsed by `search()`) carries `datasheetHref` — live: 990/1014 parts (97%), all absolute on `cdn.macom.com` (e.g. `https://cdn.macom.com/datasheets/MRF454.pdf`); PDF `HEAD`→200 `application/pdf`. www robots `Allow: /`; cdn.macom.com robots permits. Currently discarded (`macom.py:185` reads only partNumber/specs/partUrl). Impl: `datasheet_url = part.get("datasheetHref")` in `_build_candidate` — no extra request, no absolutize.
- [x] 6.9 Microchip — **verified: case 1 via the MCP**. `datasheetUrl` is returned directly by the MCP (`search_products` `product` dict AND `search_product_physical_specs` `physical`) — already fetched by `search()`, absolute (e.g. `https://ww1.microchip.com/downloads/.../MMA044AA-…-DS00004231B.pdf`), no extra request, no HTML scrape. The microchipdirect feed (hop 3) has NO datasheet field. Coverage ~87% (some parts have no `datasheetUrl`). **Compliance:** the LINK is obtained via `api.microchip.com` (serves no robots.txt → no restriction; public, no-auth, agent-built) — pulling it is unambiguously allowed. The PDF file lives on `ww1.microchip.com` whose robots is `User-agent:* Disallow:/`; carrying the URL ≠ fetching it, so the adapter supplies it (decided with user). **NB the pipeline's later PDF *fetch* of ww1 is a separate robots decision** — and note a fully-clean alternative exists: the MCP tool `search_microchip_product_documents` returns datasheet *content* (markdown, per page) straight from api.microchip.com, sidestepping ww1 entirely (a possible "datasheet-text-from-API" path if the pipeline's ww1 fetch is later disallowed).
- [x] 6.10 UMS — **verified: case 1**. Each `tr.product-row` in the `?function=<slug>` parametric page (already fetched by `search()`) carries an `a.doc-link` whose href is an ABSOLUTE datasheet PDF on the same host: `https://www.ums-rf.com/wp-content/uploads/YYYY/MM/{MODEL}-Full-….pdf` — same HTML response as the row's params, no extra request. Verified live end-to-end: the project's `datasheet_text_from_url` fetched+parsed CHA1008-99F (1390 chars). robots ALLOWS it (only `/wp-admin/` disallowed). Currently discarded — `ums.py` `_build_candidate` reads `a.product-link` + `td.characteristic-cell` but NOT `a.doc-link` (skill §6: "captured but not parsed now"). Impl (§7): read `a.doc-link` href into `datasheet_url`, inline in `search()`. **Caveat:** one fetch transiently returned `text/html` instead of the PDF (rate-limit/hiccup); a retry succeeded. `datasheet_text_from_url` has no retry → a transient miss leaves that candidate unenriched (graceful, not-verified); consider adding a retry.
- [x] 6.11 GuerrillaRF — **verified: case 2**. The datasheet link is NOT in the `amplifiers.html` table `search()` fetches — the row carries only `/products/detail/sku/{model}` + `/products/pCharts/sku/{model}` links. The datasheet lives on the DETAIL page (`/products/detail/sku/{model}`), so it needs an extra per-part fetch (like Mini-Circuits). Main datasheet URL: `https://www.guerrilla-rf.com/includes/prodFiles/{sku}/{MODEL}DS.pdf` (the `…DS` suffix = DataSheet; verified live GRF2003DS.pdf fetched+parsed by the project's `datasheet_text_from_url`, 1410 chars). robots ALLOWS it. **Caveat A — many PDF links:** the detail page has the main `…DS.pdf`, per-frequency "CustomTunes" variants, and AppNotes — §7 must pick the main datasheet, not an app note/tuning file. **Caveat B — TWO forms of the datasheet link, one is a silent-HTML trap:** the detail page carries BOTH `/products/DataSheet?sku={n}&file_name={MODEL}DS.pdf` (returns **`text/html`** — a viewer page that merely EMBEDS the PDF link) AND the DIRECT `/includes/prodFiles/{sku}/{MODEL}DS.pdf` (returns **`application/pdf`**, `%PDF`). BOTH URLs contain `…DS.pdf`, so a naive "grab the `…DS.pdf` href" can grab the HTML wrapper → `datasheet_text_from_url` fails the `%PDF` guard → silent DatasheetFetchError (cf. Mini-Circuits). Impl (§7, case 2): override `resolve_datasheet_url` — GET the detail page, extract the **direct `/includes/prodFiles/…DS.pdf`** href (NOT the `/products/DataSheet?…` wrapper), return it; do NOT fetch from `search()`. If a part exposes only the wrapper, add a second hop (GET the wrapper HTML, extract the `/includes/prodFiles/…` link inside) — verify across parts whether the direct link is always present. **Possible optimization:** URL looks constructable as `/includes/prodFiles/{model without 'GRF'}/{model}DS.pdf` (→ would become case 1, no detail fetch) — but that is a GUESS; verify across many parts before relying on it (Mini-Circuits lesson: constructed URLs can fail silently).
- [x] 6.12 Qorvo — **verified: case 1** (fetchable as-is, no robots issue). The product-list row (already scraped by `search()`) carries a `/products/d/{docid}` document link. For a datasheet doc, `GET /products/d/{docid}` serves the PDF DIRECTLY: `Content-Type: application/octet-stream`, `Content-Disposition: attachment; filename="… Data Sheet.pdf"`, body starts `%PDF-1.7`. Verified live end-to-end: da009761→QPL1840 (1 MB), and the project's `datasheet_text_from_url` fetched+parsed it (1681 chars). robots ALLOWS it (`can_fetch('*', /products/d/…)==True`; only `/*?*`, `/api`, `/*.pdsc` are disallowed). The `attachment` disposition is a browser-only hint — irrelevant to httpx; and the octet-stream content-type is handled because pdf.py's guard trusts the `%PDF` signature over Content-Type. No JS/Playwright needed. **Caveats for §7:** (a) NOT every `/products/d/` doc is a datasheet — da005874 (QPA5219) returns an HTML "Important Notice" (export-controlled/other doc); for those `datasheet_text_from_url` fails the `%PDF` guard → DatasheetFetchError → candidate stays unenriched (not-verified), graceful. (b) UNCONFIRMED (Qorvo rate-limited the probe): whether a row can carry MULTIPLE `/products/d/` links — §7 must pick the datasheet one (label/class/icon), not the first. Impl (§7): read the datasheet `/products/d/{docid}` href from the row into `datasheet_url` (case 1, inline in `search()`).
- [x] 6.13 Record the results as a case-per-source table in this change, and update each adapter's skill with the finding (the skills are where per-site retrieval knowledge lives) — table added above; a `## 1b. Datasheet link` section was inserted into all 8 existing skills (minicircuits, marki, rwmmic, threerwave, amcomusa, macom, microchip, ums). **Not covered: VectraWave, Qorvo, GuerrillaRF and Analog Devices have no skill at all** — their findings live only in this table; writing those four skills is out of scope here.

## 7. Adapters supply the datasheet URL

Sized by §6's classification. Case 1 = read inline in `search()`; case 2 = override `resolve_datasheet_url`; case 3 = nothing. Every supplied URL MUST be absolute (resolve relative hrefs against the site's base URL).

- [x] 7.1 Mini-Circuits (case 2): override `resolve_datasheet_url` — GET `dashboard.html?model={urllib.parse.quote(model, safe='')}`, take the `<a>` whose text is `DATASHEET`, absolutize the href; return `None` on any failure. Keep the existing rate guard. Do NOT fetch from `search()`.
- [x] 7.2 Mini-Circuits: switch `Candidate.url` from the robots-disallowed `modelSearch.html?model=` to the allowed `dashboard.html?model=` — the real product page, and now also what the result's product-URL field surfaces. This closes the skill's open OQ-2.
- [x] 7.3 RWM (case 1): move the datasheet link from `url` into `datasheet_url`, and give `url` the product page instead — today `url` holds the PDF (`rwmmic.py:281`), so the result's "Product URL" would show a PDF and the user would have no route to the part's page
- [x] 7.4 VectraWave (case 1): set the already-parsed per-product PDF href into `datasheet_url`
- [x] 7.5 Marki (**case 2**, per 6.4): stop discarding the `Datasheet` column — `search()` reads its `<a href>` (relative; absolutize against `_BASE_URL`) and carries it as the first-hop page. Do NOT construct it from `cand.url`, even though the two match on 123/123 rows today. Then override `resolve_datasheet_url`: GET that page behind the existing rate + Cloudflare guards, take the `<a>` whose text is exactly `Download PDF` (never the two `Online Catalog` links, which are catalogue PDFs — a valid PDF that would count as "datasheet accessible" and wrongly DROP the candidate instead of leaving it `not-verified`), and return its already-absolute `/assets/{uuid}/….pdf` href. **On any failure return `None` explicitly — never fall back to `cand.datasheet_url`** (the `base.py` default does exactly that, and here that value is an HTML page, which the pipeline would then try to parse as a PDF). Also update `marki.py:44`'s "datasheet PDFs are not fetched programmatically" note — robots allows both hops and the pipeline now fetches them
The remaining sources per §6's findings — one task per adapter (3rWave needs none, per 6.5):

- [x] 7.6.1 **Microchip (case 1) — DONE:** `_build_candidate` reads `datasheet_url = product.get("datasheetUrl") or physical.get("datasheetUrl")` from the already-fetched MCP payload (`microchip.py`); fixture gained a `datasheetUrl` on MMA044AA; tests `test_datasheet_url_from_mcp_no_extra_request` (carries the absolute link, no request) and `test_datasheet_url_absent_stays_none` (7.7 shape) pass
- [x] 7.6.2 **AmcomUSA (case 1):** read the row's LAST cell holding an `<a>` whose href ends in `.pdf` (an absolute CloudFront URL) into `datasheet_url`. Identify it as "last cell with a `.pdf` anchor", NOT by a fixed index — the column set differs per category. The column has an empty header, which is why it is skipped today (`amcomusa.py:325`)
- [x] 7.6.3 **MACOM (case 1):** `datasheet_url = part.get("datasheetHref")` in `_build_candidate` — already in the fetched `data-part` JSON, absolute, no extra request, no absolutize. The ~2% of parts without the field stay `None` (verified: those parts genuinely have no datasheet)
- [x] 7.6.4 **UMS (case 1):** read each `tr.product-row`'s `a.doc-link` href (absolute, same host) into `datasheet_url`; `_build_candidate` currently reads only `a.product-link` + `td.characteristic-cell`
- [x] 7.6.5 **GuerrillaRF (case 2):** override `resolve_datasheet_url` — GET the detail page `/products/detail/sku/{model}` and return the **direct** `/includes/prodFiles/{sku}/{MODEL}DS.pdf` href, never the `/products/DataSheet?…` wrapper (it returns `text/html`, so it would fail the `%PDF` guard silently). Both forms contain `…DS.pdf`, so matching on the filename alone is not enough. Also skip the per-frequency "CustomTunes" variants and the AppNotes on that page. `search()` makes no per-part request
- [x] 7.6.6 **Analog Devices (case 1, derived URL):** set `datasheet_url = f"https://www.analog.com/en/{model}/datasheet"` inline in `search()` — zero extra requests, since the model is already in the parametric JSON. Re-confirmed live that there is no literal link to read: the payload has 0 occurrences of `.pdf`, `datasheet` or `/media/`, and its only keys are `categoryId`/`data`/`description`/`displayValue`/`generic`/`value`. **robots: ALLOWED** — `robots.txt` is unreachable at its canonical path from this network but IS served at `https://www.analog.com/cdp/robots.txt`, and `can_fetch('*', …)` is True for both `/en/{model}/datasheet` and `/media/en/technical-documentation/data-sheets/*.pdf` (ADI explicitly disallows app-notes/design-notes/tech-articles, and sets `Crawl-delay: 5`, matching the adapter's existing delay). **Fetchability CONFIRMED (user-verified in a browser):** `https://www.analog.com/en/ADL6346B/datasheet` and `https://www.analog.com/en/HMC1131/datasheet` both open the PDF — two different families, the second a legacy Hittite part, so this is a pattern and not a one-off. (From this network every `www.analog.com` path outside the `/cdp/` prefix still dies with `RemoteProtocolError`, which is why the check had to be run elsewhere.)
- [ ] 7.6.7 **Qorvo (case 1) — BLOCKED; do not implement on guesswork.** 6.12 established that the product-list row carries a `/products/d/{docid}` link which serves the PDF directly, but NOT how to identify it among the row's other anchors, and left open whether a row can carry several. `tests/fixtures/qorvo_product_list.html` contains **0** `/products/d/` links, so neither the code nor its test can be written from what we hold. Every access attempt from this network fails (`RemoteProtocolError`; via curl/schannel a 301 arrives and then the body is dropped). **To unblock:** one successful fetch of `https://www.qorvo.com/products/product-list/` from an unfiltered network — then record the datasheet anchor's class/text/title, whether a row can carry multiple doc links, and refresh the fixture with a row that has one
- [ ] 7.7 Per-adapter test: a case-1 adapter's candidates carry an absolute `datasheet_url` from the source page, with no extra request
- [ ] 7.8 Per-adapter test (case 2): `search()` makes NO per-part request and leaves `datasheet_url` as `None`; `resolve_datasheet_url` returns the absolute PDF link from a stubbed product page; a stubbed fetch failure and a product page with no datasheet link both return `None` without raising
- [ ] 7.9 Mini-Circuits regression test: a model whose name contains `+` is URL-encoded (`%2B`) when building the product-page URL — the un-encoded form silently yields a 200 page with no datasheet link
- [ ] 7.10 Microchip: when a part's `datasheetUrl` is absent, fall back to the packaging-variant base part's link — strip a trailing `/TR`, `TR` or `E` and reuse that part's `datasheetUrl` **from the same already-fetched `_enumerate` result set**; zero extra requests, still case 1. Live, this recovers 5 of the 13 link-less parts (`MMA043PP4/TR`→`MMA043PP4`, `MMA044PP3/TR`→`MMA044PP3`, `MMA047PP4TR`→`MMA047PP4`, `MMA085PP4`/`MMA085PP4E`→`MMA085PP4/TR`) — same die, different reel/packaging, one datasheet. Do NOT use the record's `basePartNumber` field: live it holds a marketing string (`LOW-NOISE-AMPLIFIER-WIDEBAND`), not a part number. When no sibling with a link exists, leave `datasheet_url = None` — unchanged behaviour (D7 condition 1), so this can only add coverage, never remove it. Test: a link-less variant inherits its base part's URL; a link-less part with no sibling stays `None`

## 8. End-to-end tests for the pipeline

- [ ] 8.1 Gate 1 drops a candidate whose table parameter `FAIL`s (never resolved, never enriched)
- [ ] 8.2 A survivor with a site-missing param is enriched from a stubbed datasheet and returned as a `match` when the datasheet value passes
- [ ] 8.3 A survivor whose datasheet value `FAIL`s is dropped by Gate 2 (even if another requested param is unverified)
- [ ] 8.4 A survivor whose datasheet is read successfully but is silent on a site-missing param (stays `UNKNOWN`) is dropped by Gate 2 — not `not-verified`
- [ ] 8.5 A survivor whose datasheet is inaccessible (no link after resolution, or a stubbed fetch/parse/extractor failure) is returned as `not-verified` when it clears 80% coverage; the same case below 80% coverage is dropped
- [ ] 8.6 Only site-missing params are requested from the extractor (table-answered params are not re-requested)
- [ ] 8.7 A raising adapter / failing datasheet / failing resolution does not abort the run
- [ ] 8.8 The returned result exposes product name, manufacturer, product URL, and the outcome tag; it exposes neither `source` nor `datasheet_url`
- [ ] 8.9 A candidate enriched from its datasheet surfaces the datasheet's PARAMETERS in its verdicts, while the datasheet link itself stays out of the result
- [ ] 8.10 Resolution is attempted only for Gate 1 survivors with a missing param — NOT for a survivor whose params all passed from the table, and NOT for candidates dropped at Gate 1
- [ ] 8.11 The pipeline asks the producing adapter to resolve, and holds no per-site logic (a fake case-2 adapter's `resolve_datasheet_url` is called for its own candidates only)

## 9. Validate

- [ ] 9.1 Run the full test suite
- [ ] 9.2 `openspec validate add-datasheet-orchestration-pipeline --strict`
